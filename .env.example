# docker-compose.yml için PostgreSQL kimlik bilgileri.
# Bu dosyayı .env olarak kopyalayıp parolayı kendiniz belirleyin.
# .env git tarafından yok sayılır.
POSTGRES_DB=mars_rover_db
POSTGRES_USER=mars_user
POSTGRES_PASSWORD=degistirin-guclu-bir-parola
