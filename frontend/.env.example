# Gerçek token yazmayın — bu dosya GitHub'a gider.
# Kullanım: kopyalayıp frontend/.env yapın; değer backend SENTINEL_API_TOKEN ile aynı olmalı.
# Vite yalnızca VITE_ önekini istemciye verir; değişince `npm run dev` yeniden başlatın.
VITE_API_TOKEN=
